## Summary

Full project audit completing 12 fixes across three priority levels.

### Critical (all done)
- **Sitemap domain**: hardcoded `parkinson-research.vercel.app` → `NEXT_PUBLIC_SITE_URL` env var
- **Report og:url**: hardcoded domain fixed, added `og:image`
- **CI double-build**: `deploy.yml` now waits for `ci` job, no redundant rebuild
- **Duplicate security scans**: `audit.yml` merged into `ci.yml` as non-blocking `security` job (Trivy pinned to `0.54.0`)

### High (all done)
- **Standalone artifact path**: `content-validation` now uses `.next/` directly instead of broken `.next/standalone/` path
- **OG image**: fallback 1200x630 image added to layout — all pages inherit it
- **`output: 'standalone'` removed**: Vercel ignores it, was causing CI confusion
- **tsconfig target**: updated from ES2017 to ES2022 (Next.js 15 standard)

### Medium (all done)
- **Trivy pinned**: `aquasec/trivy:latest` → `aquasec/trivy:0.54.0`
- **postinstall added**: `next prepare` generates `.next/types/` to prevent mysterious CI failures
- **content-validation rewritten**: progress dots, structured output, no more silent skips

### Verification
- `npm run lint` — No ESLint warnings or errors
- `npm run typecheck` — Clean
- `npm run build` — All routes build successfully

---

**Files changed (10):**

```
.github/workflows/ci.yml     — merged audit, fixed artifact path, removed dead env var
.github/workflows/deploy.yml — added needs: [ci], removed rebuild
.github/workflows/audit.yml — DELETED (merged into ci.yml)
next.config.ts              — removed output: 'standalone'
tsconfig.json               — ES2017 → ES2022
package.json                — added postinstall
scripts/content-validation.ts — full rewrite
src/app/[lang]/layout.tsx   — added OG + twitter images
src/app/[lang]/sitemap.ts   — NEXT_PUBLIC_SITE_URL env var
src/app/[lang]/report/[date]/page.tsx — fixed og:url, added og:image
```